# frozen_string_literal: true

source "https://rubygems.org"

gem "rails", "~> 8.1"
gem "rspec-rails"
gem "simplecov", path: "../../.."

# The template languages the demo report covers alongside ERB.
gem "haml"
gem "slim"

gem "rake"

# Stdlib gems newer Rubies no longer bundle by default.
gem "logger"
gem "bigdecimal" if RUBY_VERSION >= "3.4"
gem "drb" if RUBY_VERSION >= "3.4"
gem "mutex_m" if RUBY_VERSION >= "3.4"
gem "benchmark" if RUBY_VERSION >= "4.0"
