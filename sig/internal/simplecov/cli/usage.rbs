module SimpleCov
  module CLI
    # The `simplecov help` text, split out of CLI so the dispatch module
    # stays within Metrics/ModuleLength. `cli` is the CLI module; its
    # default-path accessors resolve at call time.
    module Usage
      def self?.text: (untyped cli) -> ::String
    end
  end
end
